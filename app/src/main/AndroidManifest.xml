<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Coarse location is sufficient: GPS fix is snapped to the nearest city in CityDatabase. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Pulled in transitively by androidx.work; the app schedules only unconstrained
         (Constraints.NONE) work, so network-state access is unused. Strip it. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"
        tools:node="remove" />

    <application
        android:name=".HebrewCalendarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HebrewCalendar">

        <!-- Main Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Translucent reader overlay for Tehillim / Parsha text -->
        <activity
            android:name=".ui.reader.TextReaderActivity"
            android:exported="false"
            android:theme="@style/Theme.HebrewCalendar.Reader" />

        <!-- Foreground Service for persistent notification -->
        <service
            android:name=".service.CalendarNotificationService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="calendar_display" />
        </service>

        <!-- Boot receiver -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Date change receiver -->
        <receiver
            android:name=".receiver.DateChangeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Alarm receiver for zman transitions and service restart -->
        <receiver
            android:name=".receiver.ZmanAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hebrewcal.ZMAN_TRANSITION" />
                <action android:name="com.hebrewcal.MIDNIGHT_REFRESH" />
                <action android:name="com.hebrewcal.WIDGET_REFRESH" />
                <action android:name="com.hebrewcal.SERVICE_RESTART" />
            </intent-filter>
        </receiver>

        <!-- Glance AppWidget -->
        <receiver
            android:name=".ui.widget.HebrewDateWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/hebrew_date_widget_info" />
        </receiver>

    </application>
</manifest>
